<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Home"
            app:argType="string" />
        <action
            android:id="@+id/action_nav_home_to_nav_url_open"
            app:destination="@id/nav_url_open"
            app:enterAnim="@anim/slide_right_to_left"
            app:exitAnim="@anim/slide_left_to_right" />
    </fragment>

    <fragment
        android:id="@+id/nav_academics"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/academics/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Academics"
            app:argType="string" />
        <action
            android:id="@+id/action_nav_academics_to_nav_url_open2"
            app:destination="@id/nav_url_open"
            app:enterAnim="@anim/slide_right_to_left"
            app:exitAnim="@anim/slide_left_to_right" />
    </fragment>

    <fragment
        android:id="@+id/nav_husky_experience"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/husky_experience/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Husky Experience"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_accounts"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/accounts/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Accounts"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_notices"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/notices/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Notices"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_profile"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/profile/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Profile"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_academic_calendar"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/academic_calendar/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Academic Calendar"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_resources"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/resources/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="Resources"
            app:argType="string" />
    </fragment>

    <fragment
        android:id="@+id/nav_url_open"
        android:name="edu.uw.myuw_android.CommonWebViewFragment"
        android:label="{title}">
        <argument
            android:name="base_url"
            android:defaultValue="http://curry.aca.uw.edu:8000/"
            app:argType="string" />
        <argument
            android:name="title"
            android:defaultValue="temp"
            app:argType="string" />
        <action
            android:id="@+id/action_nav_url_open_self"
            app:destination="@id/nav_url_open"
            app:enterAnim="@anim/slide_right_to_left"
            app:exitAnim="@anim/slide_left_to_right" />
    </fragment>

</navigation>